# Server port. Nginx proxies public requests to this port.
PORT=3000

# Maximum size of one uploaded image, in bytes (20 MB).
MAX_FILE_SIZE=20971520

# Maximum files accepted in one API request. The browser currently sends one file per request.
MAX_FILES_PER_REQUEST=10

# Use production when deployed to EC2.
NODE_ENV=production
